/*Functions as a black box.
-->takes Some Input
-->Gives some Output

In-built Functions
1)console.log --->this function takes some data as input and then displays them int hte output.

-->If we have 2 different console.log(),then both of them display.print poutput in different ways.
-->we can also pass multiple values together to console.log and all of them will be on the same line.(using Comma-CSV-Comma Separated Values)

*/
console.log(10);
console.log("Hello");

let age=13;
console.log(age);

console.log(age,342,"Random text",45);